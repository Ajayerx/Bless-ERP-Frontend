# Production build environment.
# Base URL stays relative ("/api") - same-origin requests are reverse-proxied
# to the Frappe backend by web.config on the server. Do NOT set an absolute
# VITE_API_BASE_URL here: cross-origin calls would break cookie/CSRF auth.
VITE_ENABLE_MSW=false
